/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['example.com'],
  },
  // আরও কনফিগ এখানে যোগ করতে পারো
};

module.exports = nextConfig;
